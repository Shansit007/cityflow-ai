/**
 * The assistant's identity, in one place.
 *
 * WHY "SAARTHI"
 * A saarthi (सारथी) is the charioteer — the one who guides the journey while
 * the traveller decides where to go. That is exactly the relationship CityFlow
 * AI is trying to have with a commuter: it advises on the route through time,
 * it never takes the wheel. It is also a real Indian word rather than an
 * invented product noise, which suits a public-service tool.
 *
 * Changing the name is a one-line edit here; nothing else hard-codes it.
 */

export const ASSISTANT_NAME = "Saarthi";

/** Shown under the name, so the word is meaningful to everyone. */
export const ASSISTANT_TAGLINE = "your travel guide";

/** Used in page titles and navigation. */
export const ASSISTANT_FULL_NAME = `${ASSISTANT_NAME} · CityFlow AI Assistant`;
